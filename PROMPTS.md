# NMS UI 개선 프롬프트 모음

> MCP 플러그인 9개를 활용한 웹 개선 프롬프트
> 사용법: 원하는 프롬프트를 복사해서 Claude Code에 붙여넣기

---

## 1. 전체 페이지 시각 점검 (Playwright + browserloop)

### 1-1. 전체 페이지 스크린샷 + 평가
```
localhost:3000에 접속해서 로그인 후 모든 페이지를 순서대로 캡처해줘.
각 페이지마다:
1. Playwright로 스크린샷 캡처
2. 글래스모피즘 디자인 일관성 평가 (배경 blur, 테두리 투명도, 그림자)
3. 텍스트 가독성 평가 (색상 대비, 폰트 크기)
4. 여백/정렬 문제 찾기
5. 개선 필요한 부분 리스트업

페이지 순서: 로그인 → 대시보드 → 자산관리 → 그룹관리 → 실시간관제 → 장애현황 → 성능통계 → 토폴로지 → 게시판
```

### 1-2. 특정 페이지 집중 리뷰
```
localhost:3000/perf/stats 페이지를 browserloop로 캡처하고 콘솔 로그도 수집해줘.
확인할 것:
1. 스크린샷으로 시각적 문제점 찾기
2. 콘솔 에러/경고 분석
3. ECharts 렌더링 이슈 확인
4. 글래스 패널 간 간격/정렬 확인
5. 발견된 문제 즉시 수정
```

### 1-3. 다크/라이트 모드 비교
```
localhost:3000 대시보드를 다크 모드와 라이트 모드 각각 캡처해서 비교해줘.
체크포인트:
1. 글래스 효과가 두 모드에서 모두 자연스러운지
2. 텍스트 색상이 배경과 충분한 대비를 갖는지
3. 차트 컬러가 두 모드에서 모두 보이는지
4. 버튼/뱃지 색상이 모드별로 적절한지
```

---

## 2. 컴포넌트별 디자인 개선 (Flowbite + shadcn/ui)

### 2-1. 테이블 컴포넌트 리디자인
```
Flowbite MCP에서 다크 모드 테이블 컴포넌트를 참고해서 DataTable 컴포넌트를 개선해줘.
현재 파일: src/components/DataTable.jsx

개선 방향:
- Flowbite 다크 테이블 스타일 참고하되 글래스모피즘 적용
- 헤더: rgba(255,255,255,0.03) 배경 + backdrop-blur
- 행 호버: rgba(255,255,255,0.04)
- 선택 행: 인디고 글래스 틴트
- 정렬 인디케이터 시각적 개선
- 페이지네이션 버튼 글래스 스타일
비즈니스 로직은 절대 변경하지 마.
```

### 2-2. 폼 컴포넌트 개선
```
Flowbite MCP의 다크 모드 폼 요소(input, select, checkbox, radio)를 참고해서
현재 프로젝트의 폼 스타일을 개선해줘.

적용 대상:
- 로그인 폼 (Login.jsx)
- 장비 등록 다이얼로그 (AssetManagement.jsx 내 모달)
- 검색 바 (각 페이지 상단)

스타일 기준:
- Input: 글래스 배경, focus 시 인디고 글로우 링
- Select: 드롭다운 패널 글래스 효과
- Checkbox/Radio: 체크 시 인디고 그라디언트
- 에러 상태: 붉은 글로우 테두리
```

### 2-3. 모달/다이얼로그 개선
```
shadcn/ui MCP에서 Dialog 컴포넌트의 최신 API와 사용법을 확인하고,
현재 프로젝트의 모달 컴포넌트와 비교해줘.

확인 후:
1. 접근성(ARIA) 속성이 올바른지 검증
2. 키보드 네비게이션 (Escape 닫기, Tab 트랩) 작동 확인
3. 오버레이 글래스 효과 개선 (backdrop-blur-md)
4. 진입/퇴장 애니메이션 부드럽게
5. 모바일에서 모달 크기 적절한지
```

---

## 3. 차트 최적화 (ECharts MCP)

### 3-1. 대시보드 차트 테마 통일
```
ECharts MCP를 사용해서 NMS 대시보드에 최적화된 다크 테마 차트 설정을 생성해줘.

요구사항:
- 배경: 투명 (글래스 패널 위에 올라감)
- 텍스트: #94a3b8 (부제), #f8fafc (강조)
- 그리드: rgba(255,255,255,0.06)
- 시리즈 컬러: #3b82f6, #10b981, #f59e0b, #8b5cf6, #ef4444, #06b6d4, #ec4899, #84cc16
- 툴팁: rgba(15,15,35,0.95) 배경 + blur
- 범례: #94a3b8 텍스트
- 게이지: roundCap, 진행바 12px
- 라인: smooth, showSymbol false, areaStyle opacity 0.08

이 테마를 chartTheme.js로 만들어서 모든 차트에 일괄 적용해줘.
```

### 3-2. 트래픽 차트 개선
```
ECharts MCP로 트래픽 IN/OUT 라인 차트의 최적 옵션을 생성해줘.

데이터: 5분 간격 시계열, IN/OUT BPS 값
요구사항:
- IN: #06b6d4 (시안), OUT: #8b5cf6 (보라)
- 영역 그라디언트 (상→하 투명)
- 피크 포인트 markPoint (원형, 8px, 흰 테두리)
- 줌 가능한 dataZoom (하단 슬라이더)
- 반응형 (컨테이너 크기 따라 자동 조절)
- 다크 배경 최적화
```

### 3-3. 게이지 차트 개선
```
ECharts MCP로 CPU/Memory 게이지 차트의 최적 옵션을 만들어줘.

현재 문제:
- 게이지가 너무 단순함
- 값 변화 시 애니메이션 없음
- 위험 구간 색상 구분 없음

요구사항:
- 0~60: #10b981 (정상/초록)
- 60~80: #f59e0b (주의/노랑)
- 80~100: #ef4444 (위험/빨강)
- 진행바 roundCap, 14px
- 중앙 값: JetBrains Mono, 28px, bold
- 하단 라벨: Inter, 12px
- 값 변화 시 부드러운 트랜지션
```

---

## 4. 접근성 검사 (A11y MCP)

### 4-1. 전체 사이트 접근성 감사
```
A11y MCP로 localhost:3000의 주요 페이지를 WCAG 2.1 AA 기준으로 검사해줘.

검사 대상:
1. 로그인 페이지
2. 대시보드
3. 자산관리 (테이블 + 모달)
4. 실시간 관제
5. 성능 통계

특히 글래스모피즘 디자인에서 발생하기 쉬운 문제:
- 반투명 배경 위 텍스트 대비 (4.5:1 이상인지)
- 포커스 인디케이터 가시성
- ARIA 레이블 누락
- 키보드 접근성

발견된 문제는 심각도별로 정리하고 수정 코드까지 제시해줘.
```

### 4-2. 색상 대비 집중 검사
```
A11y MCP로 현재 디자인 시스템의 색상 대비를 전수 검사해줘.

확인할 조합:
- #f8fafc (주텍스트) on rgba(255,255,255,0.03) 배경
- #94a3b8 (부텍스트) on rgba(255,255,255,0.03) 배경
- #64748b (뮤트) on rgba(255,255,255,0.03) 배경
- #10b981 (성공 뱃지) on rgba(16,185,129,0.15) 배경
- #ef4444 (위험 뱃지) on rgba(239,68,68,0.15) 배경
- 버튼 텍스트 on 그라디언트 배경

WCAG AA (4.5:1) 미달인 조합은 대안 색상을 제안해줘.
```

---

## 5. 아이콘 개선 (Icons8 MCP)

### 5-1. NMS 전용 아이콘 세트 구성
```
Icons8 MCP에서 NMS 대시보드에 적합한 아이콘을 찾아줘.

필요한 아이콘 (라인 스타일, 24px, 단색):
- 네트워크 장비: 라우터, 스위치, 서버, 방화벽, AP
- 상태: 정상(체크), 장애(경고), 다운(X), 점검중(렌치)
- 메트릭: CPU, 메모리, 트래픽(화살표), 응답시간(시계)
- 네비게이션: 대시보드, 자산관리, 관제, 장애, 성능, 토폴로지, 설정
- 액션: 추가, 편집, 삭제, 검색, 필터, 새로고침, 다운로드, PDF

각 아이콘의 SVG 코드를 제공하고,
src/assets/icons/ 폴더에 React 컴포넌트로 정리해줘.
```

### 5-2. 사이드바 아이콘 교체
```
현재 사이드바가 lucide-react 아이콘을 쓰고 있는데,
Icons8 MCP에서 NMS에 더 적합한 전문적인 아이콘을 찾아서 교체 제안해줘.

현재 → 개선 방향:
- LayoutDashboard → 네트워크 대시보드 느낌
- Server → 실제 서버 랙 느낌
- Shield → 보안/방화벽 느낌
- Activity → 실시간 모니터링 느낌
- BarChart3 → 통계/분석 느낌
- Network → 토폴로지 느낌

스타일: 라인 아이콘, 현재 디자인과 어울리는 것
```

---

## 6. 성능 최적화 (Lighthouse MCP)

### 6-1. 성능 감사 + 개선
```
Lighthouse MCP로 localhost:3000 대시보드 페이지의 성능을 측정해줘.

측정 항목:
1. LCP (Largest Contentful Paint) — 목표: 2.5초 이내
2. FID (First Input Delay) — 목표: 100ms 이내
3. CLS (Cumulative Layout Shift) — 목표: 0.1 이내
4. TTI (Time to Interactive)
5. Total Bundle Size

측정 후:
- 점수가 낮은 항목의 원인 분석
- 구체적인 최적화 코드 제시
- React.lazy 적용 필요한 컴포넌트 식별
- 이미지/폰트 최적화 방안
- 불필요한 리렌더링 찾기
```

### 6-2. ECharts 번들 최적화
```
Lighthouse MCP로 번들 크기를 분석하고, ECharts 트리쉐이킹을 적용해줘.

현재: import * as echarts from 'echarts' (전체 번들)
목표: 사용하는 차트 타입만 임포트

사용 중인 차트 타입:
- line (트래픽 추이)
- bar (TOP 10)
- gauge (CPU/MEM)
- pie (분포)

echarts/core + 필요한 모듈만 임포트하는 코드로 변환해줘.
```

---

## 7. SVG/커스텀 그래픽 (SVGMaker MCP)

### 7-1. 빈 상태 일러스트 생성
```
SVGMaker MCP로 NMS 페이지의 빈 상태(Empty State) SVG를 만들어줘.

필요한 일러스트:
1. "데이터 없음" — 빈 차트 + 물음표
2. "장비 없음" — 비어있는 서버 랙
3. "장애 없음" — 체크마크 + 방패
4. "검색 결과 없음" — 돋보기 + 빈 문서

스타일:
- 단색 라인 드로잉
- 메인 컬러: #6366f1 (인디고)
- 서브 컬러: #94a3b8 (슬레이트)
- 크기: 200x200px
- 미니멀 + 모던
```

### 7-2. 로고/브랜드 마크
```
SVGMaker MCP로 NMS 시스템 로고를 만들어줘.

컨셉:
- 네트워크 노드 3개가 연결된 형태
- 글래스모피즘 느낌 (반투명 + 그라디언트)
- 인디고~보라 그라디언트 (#6366f1 → #8b5cf6)
- 심플하고 현대적
- 사이드바 상단에 들어갈 32x32 사이즈
```

---

## 8. 종합 개선 워크플로우

### 8-1. 페이지 단위 리디자인 사이클
```
[페이지명] 페이지를 아래 순서로 개선해줘:

Step 1 — 현황 파악
- Playwright로 현재 페이지 캡처
- browserloop로 콘솔 에러 수집
- A11y MCP로 접근성 문제 수집

Step 2 — 디자인 개선
- Flowbite 다크 컴포넌트 참고
- 글래스모피즘 디자인 시스템 적용 (CLAUDE.md 11절)
- Icons8에서 적절한 아이콘 적용

Step 3 — 차트 개선 (해당 시)
- ECharts MCP로 최적 옵션 생성
- 다크 테마 + 반응형 적용

Step 4 — 검증
- Playwright로 개선 후 캡처 + 비교
- A11y 재검사 (대비 문제 해결 확인)
- Lighthouse 성능 측정

Step 5 — 보고
- 변경 전/후 비교
- 개선 항목 리스트
- 잔여 이슈

비즈니스 로직, API 호출, 상태 관리는 절대 변경하지 마.
스타일링(CSS 클래스, 인라인 스타일)만 변경해.
```

### 8-2. 전체 사이트 디자인 품질 점검
```
모든 MCP를 활용해서 NMS 전체 디자인 품질을 점검해줘:

1. [Playwright] 모든 페이지 스크린샷 수집
2. [A11y] WCAG 2.1 AA 전수 검사
3. [Lighthouse] 주요 페이지 3개 성능 측정
4. [browserloop] 콘솔 에러/경고 전수 수집

점검 결과를 아래 형식으로 정리:

| 페이지 | 시각 점수 | 접근성 점수 | 성능 점수 | 콘솔 에러 | 주요 이슈 |
|--------|----------|-----------|----------|----------|----------|

각 페이지별 Top 3 개선 사항과 예상 난이도(상/중/하)를 제시해줘.
```

### 8-3. 모바일 반응형 점검
```
Playwright의 디바이스 에뮬레이션으로 아래 해상도에서 주요 페이지를 캡처해줘:

- Desktop: 1920x1080
- Laptop: 1366x768
- Tablet: 768x1024 (iPad)
- Mobile: 375x812 (iPhone)

각 해상도에서:
1. 레이아웃 깨짐 확인
2. 사이드바 동작 확인 (모바일에서 접기)
3. 테이블 가로 스크롤 확인
4. 차트 크기 자동 조절 확인
5. 터치 타겟 크기 확인 (최소 44x44px)

문제 발견 시 Tailwind 반응형 클래스로 수정해줘.
```

---

## 9. 긴급 디버깅 프롬프트

### 9-1. 화면 깨짐 긴급 수정
```
[페이지URL] 페이지가 깨져보여.
browserloop로 스크린샷 + 콘솔 로그를 동시에 수집하고,
원인 분석 후 즉시 수정해줘.
CSS만 건드리고 로직은 변경하지 마.
```

### 9-2. 차트 안 나오는 문제
```
[페이지URL]의 [차트명] 차트가 안 보여.
browserloop로 콘솔 에러를 수집하고,
ECharts MCP로 올바른 옵션 구조를 검증한 후 수정해줘.
```

### 9-3. 특정 컴포넌트 스타일 비교
```
Playwright로 아래 두 URL을 캡처해서 비교해줘:
- 기존: localhost:3001/[페이지]
- 신규: localhost:3000/[페이지]

차이점을 분석하고, 기존이 더 나은 부분은 신규에 반영해줘.
```
